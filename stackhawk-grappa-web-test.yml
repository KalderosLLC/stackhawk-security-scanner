app:
  applicationId: "8dfb96a0-ef31-467b-b616-24156c9b858a"
  env: "test"
  host: "https://ce-web-test.azurewebsites.net"

hawk:
  spider:
    base: true
    maxDuration: 15
    
  # Uncomment if authentication is needed
  # authentication:
  #   type: "form"
  #   loginPath: "/login"
  #   usernameField: "username" 
  #   passwordField: "password"
  #   username: "${GRAPPA_WEB_TEST_USERNAME}"
  #   password: "${GRAPPA_WEB_TEST_PASSWORD}"
